/*
 * configration for Github About Page
 * 
 * https://github.com/AndreLion/about
 * 
 * This configuration file is formatted in JSON.
 * 
 */
var _CUSTOMSIZE_CONFIGURATION_ = {

	domain : {
		'yourdomain':'your_github_user_id'
	},
	// Extend User Information
	userinfo : {
		}
	},

	// the repos you recommend. A thumbup icon will be shown in the repo card
	recommend : {
		'about' : true,
		'gollum' : true
	},

	//the image will be inserted into the repo card
	image : {
	},

	//the repo will not be listed in your Github About Page
	ignore : {
		'fork-update' : true
	}
}
